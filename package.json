{
  "name": "@zero-nonsense-dev/releasescribe-action",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx ncc build src/index.ts -o dist",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev:test": "tsx ../test-harness/local-run.ts",
    "test": "vitest run",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.6.3",
    "vitest": "^1.6.1",
    "@types/node": "^20.12.7"
    "tsx": "^4.7.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.1",
    "@types/node": "^20.12.7"
  }
}